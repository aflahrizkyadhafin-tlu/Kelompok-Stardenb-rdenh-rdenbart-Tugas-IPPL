import 'package:mykurir/pages/opsi_hapus_akun/components/body.dart';
import 'package:flutter/material.dart';

class OpsiHapusAkunScreen extends StatelessWidget {
  const OpsiHapusAkunScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SafeArea(child: Body()));
  }
}
